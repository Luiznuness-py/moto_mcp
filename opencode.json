{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "moto-mcp": {
      "type": "local",
      "command": ["poetry", "run", "python", "-m", "mcp_server.server"]
    }
  },
  "provider": {
    "ollama": {
      "npm": "@ai-sdk/openai-compatible",
      "name": "Ollama (local)",
      "options": {
        "baseURL": "{env:MOTO_MCP_OPENCODE_OLLAMA_BASE_URL}"
      },
      "models": {
        "qwen3:14b": {
          "name": "Qwen3 14b (editar aqui pro modelo baixado no Ollama — confirmado com tool-calling funcionando via Ollama; ver docs/mcp_server.md)"
        },
        "qwen2.5-coder:14b": {
          "name": "Qwen2.5 Coder 14b (NAO usa tool-calling neste Ollama — confirmado que nao emite tool_calls estruturado; bom so pra tarefa de codigo sem precisar das tools do moto-mcp)"
        }
      }
    },
    "anthropic": {
      "options": {
        "apiKey": "{env:ANTHROPIC_API_KEY}"
      }
    }
  },
  "agent": {
    "moto": {
      "description": "Agente minimo pra consultar o moto-mcp com modelo local — desliga as tools nativas que nao precisamos, reduzindo o tamanho do prompt (achado real: prompt de 9040 tokens sendo truncado pra 2050 sem isso) e a confusao do modelo sobre qual tool usar.",
      "mode": "primary",
      "tools": {
        "bash": false,
        "edit": false,
        "write": false,
        "task": false,
        "todowrite": false,
        "webfetch": false,
        "glob": false,
        "grep": false
      }
    }
  }
}
